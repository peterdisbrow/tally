'use strict';
// Pack-time DSN for packaged builds. Keep empty in git.
// `scripts/write-embedded-sentry-dsn.js` fills this from ELECTRON_SENTRY_DSN
// during electron-builder. Never commit a real DSN.
module.exports = '';
